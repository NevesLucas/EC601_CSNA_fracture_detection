# To run: conda env create -f environment-lnx-cuda.yml
name: csna
channels:
    - conda-forge
dependencies:
    - python=3.6
    - pip
    - Pillow
    - matplotlib
    - numpy
    - pandas
    - pip:
        - torch
        - torchvision
        - kaggle
        - pydicom
        - opencv-python
        - nibabel
