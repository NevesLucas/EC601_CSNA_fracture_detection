# To run: conda env create -f environment-lnx-cuda.yml
name: csna
channels:
    - conda-forge
dependencies:
    - python=3.6
    - pip
    - Pillow
    - matplotlib
    - numpy
    - pandas
    - cudatoolkit=11.6
    - pip:
        - pytorch
        - torchvision